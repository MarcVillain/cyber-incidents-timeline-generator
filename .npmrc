message="misc(release): publish version %s"

# The build runs TypeScript 7, whose bin is an extensionless file inside a "type":"module" package, and
# only Node 22.13 and later can load one. Without this, an older Node installs happily and fails later
# inside tsc with an error that says nothing about the Node version.
engine-strict=true
